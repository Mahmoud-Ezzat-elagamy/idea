{
    "$schema": "https://opencode.ai/config.json",
    "mcp": {
        "laravel": {
            "type": "local",
            "command": ["php", "artisan", "mcp:start", "IdeaServer"],
            "enabled": true
        }
    }
}
