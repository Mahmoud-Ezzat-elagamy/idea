[phases.setup]
nixPkgs = ["...", "php84", "php84Packages.composer", "nodejs_20"]

[phases.install]
cmds = ["echo RUNNING_CUSTOM_INSTALL", "rm -f package-lock.json", "npm install"]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=8080"
